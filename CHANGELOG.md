# CHANGELOG / 作業ログ

このプロジェクトは「5人のアドバイザー方式」で自律開発します。日々の実装・テスト・レビュー・コミットは確認待ちせず進め、完成形のみ浅井が確認します。

各エントリは「議長の結論 / 実装内容 / テスト結果 / 次に自動で進める内容」を記録します。

---

## 2026-07-28 — Netlify デプロイ失敗の修正（案A: ビルド不要構成に統一）

**議長の結論**：**案A（ビルド不要構成）を採用**。Netlify UI に旧プロジェクトの設定（build command `node tools/site.mjs` / publish `_site`）が残っていたが、このリポジトリに `tools/site.mjs` は存在せず `MODULE_NOT_FOUND` で失敗していた。Ver2.0 は静的ファイルのみ（index.html / game.js / words.js / sw.js）でビルド工程を必要としないため、存在しないビルドを再現するのではなく、実態に合わせてビルドを廃した構成へ統一する。5人会議の評価：
- 反対役／前提破壊役：「案Bはビルドスクリプトという壊れる部品をわざわざ増やす。生成物が入力と同一なら、そのビルドは存在意義がない」。案Bは Node バージョン依存・生成漏れという新しい故障点を持ち込むため不採用。
- 実行役：`netlify.toml` はファイル設定が UI 設定より優先されるため、UI を触らずコミットだけで恒久的に解決できる。最短かつ可逆。
- 拡張役：将来ビルドが必要になった時点で `netlify.toml` の1行を変えれば移行できるので、案Aは将来を塞がない。

**実装した内容**：
- `netlify.toml` を新規追加。`build.publish = "."`（リポジトリ直下を直接配信）、`build.command` は UI の旧コマンドを打ち消す no-op を明示指定（空文字だと UI 設定へ戻るため）。
- `/sw.js` と `/index.html` に `Cache-Control: public, max-age=0, must-revalidate` を設定。古い Service Worker がキャッシュに居座って次のVer更新が届かなくなるのを防ぐ。
- 新規ファイル追加のみで、ゲーム本体のコードは一切変更していない。

**テスト結果**：
- ローカルとGitHubのファイル一覧を照合 → **完全一致**（8ファイル、同一コミット `a8da545`）。`tools/site.mjs`・`_site` はどちらにも存在しないことを確認し、存在しないファイルを前提にしていない。
- TOML 構文検証（依存追加なしの自作パーサ）→ パース成功。`build.command` 定義あり／`publish == "."`／設定値に `site.mjs` 参照なし／`/sw.js` の no-cache ヘッダあり、の4アサーション PASS。
- **Netlify と同じビルドコマンドをローカル実行** → exit code 0。
- Netlify のクローンを再現（コミット予定ツリーを `git archive` で展開）し、その publish ディレクトリを実際に配信 → index.html / game.js / words.js / sw.js すべて HTTP 200。
- **配信物そのものに対して自動テスト実行 → 17 / 17 PASS**。加えて実プレイ確認（出題→正解で距離加算→次問へ遷移、横スクロールなし）。

**Gitブランチとコミット**：`ver2.0` に追加コミット。force push はしていない（通常の `git push`）。`main` は未変更。

**次に自動で進める内容**：デプロイ成功を確認後、演出強化（ステージ別の障害物・敵）、難易度カーブ調整、`words.js` の単語拡充へ進む。

---

## 2026-07-28 — Ver2.0 全面改修（3レーン英単語ランニングアドベンチャー）

**議長が採用したゲーム構成**：4択クイズを廃止し、主人公後方視点の疑似3Dランナーへ全面移行。画面中央に英単語、奥へ伸びる**3本の道**に日本語訳を1つずつ配置し、道＝答えとする。視線は常に画面中央に固定（独立した回答ボタンは置かない）。正解でそのまま走り抜け、不正解でライフ減。連続正解で加速し、距離でステージが変化、一定距離でボス。5人会議の判定：
- 出題形式は「中央=英単語 / 3本の道=日本語訳」を採用（走行中の視認速度は短い日本語の方が速い、既存単語データ構造とも一致、という理由で英→日方向に決定）。
- 反対役/前提破壊役の「視線を散らす要素を削る」に従い、HUD（距離・ライフ・コンボ）は端に最小化、重要情報は上部に置かない。
- 部外者役の「初見で操作と正解の道が分かるか」に対応し、各道に「◀左/▲中央/右▶」キーと大きな訳語を表示、遊び方パネルを用意。
- 拡張役の記録共有/ランキング/多教科展開は将来課題として保留（単語データを`words.js`に分離し拡張しやすい構造だけ先に用意）。

**廃止した旧仕様**：4択クイズ画面、画面下部の独立回答ボタン、旧レイアウト（上部を見る必要のある情報配置）。単一HTML内に全て詰め込む構成も廃止し、`index.html`/`game.js`/`words.js`/`sw.js`へ分割。

**実装した内容**：
- 疑似3D 3レーンランナー（Canvas。地平線・遠近圧縮・流れるレーン線・パララックス丘・オリジナルのマント姿冒険者スプライト）。
- 3択出題を道へ統合（正解位置は毎回ランダム、直近単語の連続回避）。
- 正解演出のバリエーション（橋を跳ぶ/スライド/ダッシュ/岩越え/敵突破）と失敗演出（落下/衝突/つまずき/激突）＋短い一言バナー、少し笑えてやり直したくなるトーン。
- 操作：タップ（左/中央/右3分割）・スワイプ（←/↑/→）・矢印キー・数字キー・各道ラベル直接タップ。タップのみで完結。
- レベル/距離/コンボ/ライフ接続。5コンボで加速、10コンボでオーラ＋ファンファーレ。制限時間はレベルで短縮。
- ステージ（0=古代遺跡/300=密林/600=洞窟/1000=雪山/1500=火山遺跡）で背景・道・光が変化。
- ボス（300/600/1000m、3問正解で撃破、操作は3択のまま、HPバー表示）。
- 学習設計：間違えた単語を結果画面に表示し、後で再出題（直近再出題は抑制）。到達距離と正答率で難易度自動調整、単語難易度d1–4。
- ヒント（📜巻物=誤答1本封鎖で2択 / ⏳時の砂=分岐をゆっくり / 👁魔法の目=正解を一瞬発光）。親指の届く下部に配置し中央視線を邪魔しない。
- Service Worker（`sw.js`、キャッシュ `wqr-v2.0.0`、network-first＋古いキャッシュ自動削除）でオフライン対応。
- `?selftest=1` で17項目の自動テスト（左下にPASS/FAILバッジ）。

**テスト結果**：
- 自己テスト **17 / 17 PASS**（単語データ健全性、ステージ閾値、3択の一意性と正解対応×200、正解位置ランダム分布、制限時間の単調減少と下限、正解で距離・コンボ増、不正解でライフ減・コンボ0・ミス記録、巻物ヒント）。
- ブラウザ実機確認（375/430px）：タイトル→プレイ→正解（距離0→21m・緑ハイライト・粒子・突破バナー）→不正解（ライフ減・失敗バナー）→ライフ0→結果画面（距離/ベスト/最大コンボ/まちがえた単語一覧）→ベスト永続化 まで一連の動作を確認。コンソールエラーなし。横スクロールなし（scrollW==clientW）。
- 構文チェック：`node --check` で words.js / game.js / sw.js いずれもOK。
- 補足：ブラウザプレビューは非表示時にrequestAnimationFrameを停止するため、キャプチャ間で見かけ上ゲームが止まる。これはプレビュー環境の仕様であり、フォーカスされた実機タブでは60fpsで進行する（デバッグフックによる同期実行でラウンド遷移も検証済み）。

**Gitブランチとコミット**：復旧点タグ `v1.0-pre-ver2` を作成し、`ver2.0` ブランチで実装。`main` は未変更（テスト通過後に反映予定）。

**まだ仮表現の部分**：
- 主人公・ボス・門・背景はCanvas図形の軽量プレースホルダー（正式なオリジナルアート素材は未確定。特定作品のコピーはしていない）。
- 突破/失敗演出はキャラの動き＋粒子＋バナーの簡易版（本格スプライトアニメは今後）。
- ステージ切替は色・丘の変化が中心（障害物や敵のバリエーションは今後追加）。
- ランキング/記録共有/多教科展開は未実装（拡張しやすい構造のみ用意）。

**次に自動で進める内容**：
- 演出の強化（ステージ別の障害物・敵、突破アニメの作り込み）と難易度カーブの調整。
- 単語数の拡充（`words.js` へ追記）と、間違え単語の復習ロジックの精緻化。
- push は GitHub 書き込み認証が必要なため保留（浅井の認証後に `ver2.0` を push、確認後 `main` へ）。

---

## 2026-07-28 — プロジェクト立ち上げ & GitHub 接続

**議長の結論**：前回作成の HTML（Netlify 公開版）を唯一の正本ソースとして採用し、単一ファイル self-contained 構成のまま新規リポジトリで管理を開始する。ローカル環境に該当フォルダは存在しなかったため、公開 HTML を取得して初期化した。

**実装内容**
- 作業ディレクトリ `~/word-quest-run` を新規作成（KENKABA とは完全に分離）
- 公開版 HTML を `index.html` として取得・配置（73KB、外部 CDN・スクリプト依存ゼロを確認）
- `README.md` / `.gitignore` / `CHANGELOG.md` を追加
- `git init` → `main` ブランチ → 初回コミット
- リモート `origin = https://github.com/kenkaba/word-quest-run.git` を接続

**テスト結果**
- ローカル配信（HTTP 200）でタイトル画面が 375px モバイルで正常描画、コンソールエラーなし
- 「冒険をはじめる」→ ゲーム画面遷移 OK（DISTANCE/LEVEL/HP/敵/4択出題を確認）
- 正答で距離加算・ベスト更新・次問(Q2)遷移まで動作確認（コアループ健全）

**次に自動で進める内容**
- push は GitHub 書き込み認証が必要なため保留（浅井の対応後に実施）
- 認証完了後、5人方式の初回ラウンドとして「継続率・分かりやすさ・共有性」を軸に小さな改善候補を洗い出し、1件ずつ実装予定
